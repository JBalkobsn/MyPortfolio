import React from "react";
import { Col, Row } from "react-bootstrap";
import { useNavigate } from "react-router-dom";

const TileSection = () => {
  return (
    <Row
      className="h-20"
      style={{ backgroundColor: "white", width: "100vw", height: "53vh" }}
    >
      <a>Jakob Baltes</a>
    </Row>
  );
};

export default TileSection;
